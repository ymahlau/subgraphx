name: subgraphx
channels:
  - conda-forge
  - pytorch
  - defaults
dependencies:
  - python=3.11
  - pip
  - swig
  - git
  - pytorch==2.2.0
  - pip:
    - numpy
    - matplotlib
    - scipy
    - torch_scatter
    - torch_sparse
    - torch_cluster
    - torch_spline-conv
    - torch_geometric
    - dive-into-graphs

